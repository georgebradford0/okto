# React Native + gluestack/NativeWind pull strict peer ranges that npm's strict
# resolver rejects (e.g. reanimated<->worklets). legacy-peer-deps is the standard
# setting for RN workspaces and keeps local installs and CI consistent.
legacy-peer-deps=true

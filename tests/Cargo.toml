[package]
name    = "okto-tests"
version = "0.0.1"
edition = "2021"

[dev-dependencies]
okto-core = { path = "../core" }
snow           = "0.9"
tokio          = { version = "1", features = ["full"] }
anyhow         = "1.0"

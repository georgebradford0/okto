apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rulyeh-data
  namespace: claudulhu
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rulyeh
  namespace: claudulhu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rulyeh
  template:
    metadata:
      labels:
        app: rulyeh
    spec:
      serviceAccountName: rulyeh
      containers:
        - name: rulyeh
          image: ghcr.io/georgebradford0/rulyeh:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
            - containerPort: 9000
              name: noise
          env:
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: claudulhu-secrets
                  key: anthropic-api-key
            - name: GH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: claudulhu-secrets
                  key: gh-token
                  optional: true
            # Optional: AWS credentials for remote EC2 provisioning
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: claudulhu-secrets
                  key: aws-access-key-id
                  optional: true
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: claudulhu-secrets
                  key: aws-secret-access-key
                  optional: true
            - name: AWS_DEFAULT_REGION
              value: "us-west-2"
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: rulyeh-data
---
# Internal HTTP access for children messaging rulyeh
apiVersion: v1
kind: Service
metadata:
  name: rulyeh
  namespace: claudulhu
spec:
  selector:
    app: rulyeh
  ports:
    - port: 8000
      targetPort: 8000
      name: http
---
# External Noise tunnel access
apiVersion: v1
kind: Service
metadata:
  name: rulyeh-noise
  namespace: claudulhu
spec:
  type: NodePort
  selector:
    app: rulyeh
  ports:
    - port: 9000
      targetPort: 9000
      nodePort: 30090
      name: noise
